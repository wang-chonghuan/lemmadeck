# Grill

## Questions and decisions

1. **Can the 151 branch be merged or cherry-picked wholesale?**
   No. Its base predates STEMROBIN-157 and its branch contains obsolete shared code relative to
   `0475a611...`. The observed diff shows that only the `7a` resource subtree belongs in this fix.

2. **What is the authoritative treatment of the two formula defects?**
   The scans show `a+b+c` on physical page 12 and a left denominator `x-1` on physical page 13.
   Engineering and lesson-skill contracts require the faithful layer to preserve those printed
   facts and the modern layer to correct them only through source-bound errata.

3. **May the two lessons be inserted into the live database for product-route acceptance?**
   No. The live read found no target rows, and the ticket does not authorize content writes.
   Verification will use the official local lesson renderer, real MathLive edits, the production
   judge, and actual localhost components on already published content. The missing target product
   routes remain explicitly pending publication.

4. **Which image sample minimizes cost while exercising the current pipeline?**
   Existing `5m` `fig-67` is a real scanned coordinate-grid figure already embedded in
   `math5-c1-s2-n16`, has a deterministic FigureSpec, and needs no paid generation. It exercises
   grid completeness, labels, point relations, mobile width, semantic colors, lesson embedding,
   and the real app surface.

5. **Does the route cross a Charter redline?**
   No. Product resources stay under `ssot-resources`; scratch stays under the ticket `tmp`;
   dependencies, schema, Charter, infrastructure, production data, credentials, and generated
   files owned by the app build are unchanged. No paid call, merge, close, deploy, or real learner
   write is planned.

6. **What remains deliberately unclaimed?**
   Production publication of the two lessons, their actual `/card/...` rows, generated/hybrid image
   modes, other diagram classes, and ten-volume readiness. These require separate evidence or
   authorization.
