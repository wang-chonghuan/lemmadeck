# STEMROBIN-139 Rework

## 2026-09-16 — 第八课图 35

**Human ask**

> 第八课的这个图是咋回事，没用新方法生成吗

**What changed**

- 确认图 35 已走 `figure-spec@2`，但原门禁允许生成器漏写最上方成员点和标签的
  `inside` 断言，因此越界图被错误放行。
- 重新排布图 35 的全部集合成员，保持五条平方映射不变；审计并同步修复存在同类遗漏的
  图 36、40。
- 校验器现在从 `*-set`、`*-<index>`、`*-<index>-label` 稳定 ID 推导关系图全部成员，
  强制每个成员点和标签声明正确容器，并检查渲染 footprint 的实际边界净距。
- 增加遗漏 containment 的回归测试，纠正技能文档中 `inside` / `connects` 的旧字段说明，
  重新生成 SVG、render/review 哈希证据，并通过课程发布器重发第 8、9 课。

**Criteria rechecked**

- AC1：图 35、36、40 的当前 FigureSpec、render、review 和映射关系通过；第 8、9 课发布门禁通过。
- AC2：桌面 `1440x960` 与手机 `390x844` 产品检查通过；图 35 标签实际为 `17.82px`，
  无标签裁切或重叠，主题和打印检查保持通过。
- AC3：图片技能 24 项测试通过，新增测试证明关系图漏写任一成员或标签 containment 会失败；
  修复后的图通过同一校验。
- AC4：资源审计、教材校验、app 82 项测试和生产构建通过；课程技能 38 项测试通过。

**Net effect**

冻结 handoff 的图片架构与发布契约不变；本轮堵住了 `figure-spec@2` 的 containment
漏检，并将第 8、9 课的同类关系图修复到该更严格契约。
