# STEMROBIN-157 Rework

修复审查指出的分式作用域、真实 MathLive 编辑输出和来源绑定断裂问题，并确保错误的
非空来源引用在 prepare、finalize 与发布前重验中均被拒绝。

## 人工与审查要求

- Plane 审查评论 `d055fb1d-9151-4efb-9c47-87847779896d` 要求修复三个 P1：
  分式规范化不得丢失乘除作用域；正式 MathLive gate 必须读取真实编辑后的序列化；
  本课勘误的来源绑定断裂或原式漂移不得被静默忽略。还要求用保留的 7a 第 13 页
  原书片段补充正式链路证据。
- Plane 定向复验评论 `45311364-67a7-4931-b0db-77e821da106f` 确认前两项及缺引用恢复
  已通过，但要求继续封闭“非空但错误的 source reference”绕过：每个非空引用都必须
  对照权威页流和正式装订身份，只有权威绑定确认属于其它课的勘误才能忽略。
- Plane 独立复验评论 `bc66c2f3-e9a9-4c40-bb18-828e2044fd63` 确认提交 `a4fa1e2`
  已通过全部定向复验。用户随后在评论 `e5ef79e2-f1d6-4c5d-a27e-803ed004c188`
  明确批准关闭本票，将有效 finish 改为 `auto-merge`，且不授权部署、课程发布或生产写入。

## 修改

- 精确答案规范化保留分子和分母的完整作用域，不再把连续字母乘积视为单个原子；
  Unicode 与 LaTeX 希腊字母使用统一映射，既有 numeric / expression 判分语义不变。
- MathLive checker 在隔离页面内通过真实键盘插入和删除触发编辑，检查 input 事件、
  编辑后序列化和值恢复，并记录有限的聚焦重试。
- 勘误准备始终从权威页流按正式 assembly 规则重建当前 lesson 的目标绑定。缺失的旧
  `source_refs` / `pages` 可恢复；非空引用必须与权威目标的完整引用集合一致，否则明确
  失败并要求重新运行 assemble。勘误只在权威绑定确认不属于当前 lesson 时忽略。
- lesson / answer 技能、契约、示例和持久测试已原位替换不完整或过时说明，没有保留第二套
  操作路径，也没有提交 STEMROBIN-151 的课程产物。

## 复验

- MathLive 共享样例与审查反例 13/13 通过；151 保留的 77 个 exact 候选全部通过。
  分式作用域错误输入被拒绝，Greek 输入经真实编辑后的 LaTeX 序列化可正确判定。
- 勘误矩阵通过：正常绑定通过；raw 原式漂移在 prepare 失败；缺引用旧课可恢复且未修正
  时 finalize 失败；错误非空引用在 prepare 失败。旧的错误提升副本在 finalize 和
  `validate_publish.py` 均失败。
- 保留的真实 7a 第 13 页样本通过正式页面收口、装订、现代版 prepare/finalize 和
  发布前重验；忠实层保留分母 `x-1`，现代层仅通过来源绑定勘误改为 `x^2-1`。
- 持久检查通过：lesson Python 70 项、answer Python 14 项、HTML fragment Node 测试、
  app 106 项测试、资源审计、教材校验和生产构建。桌面与手机产品作答检查继续通过，
  无答案泄漏、页面错误或横向溢出。
- Cap4 最终机械防线通过：资源审计、教材校验（16 个 PDF、17 册、510 课、407 条图
  记录）、app 106 项测试和生产构建全部通过；仅有既有的 chunk size 警告。

## 相对首次交付的净变化

冻结 handoff 的总体架构和验收结论不变。返工补齐了真实 MathLive 编辑序列化、分式运算
范围，以及来源绑定从“缺失时恢复”扩展到“所有非空引用均与权威装订身份核对”的机械保证。
没有依赖、数据库结构、生产数据、Charter、部署配置或 STEMROBIN-151 持久内容变化。
