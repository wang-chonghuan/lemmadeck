# AC1：当前入口唯一

检查：对仓库入口、charter、活动技能和模块文档执行限定范围检索，并读取路由文字。

通过条件：这些活动入口一致把 `ld-s10y-lesson` 声明为数学课程生产主流程，并把现代图、答案、发布和产品访问连接到对应现行能力。

# AC2：旧生成流程退出活动范围

检查：在排除冻结历史和本票记录后，检索 `sr-math-lesson`、旧 math ledger 路径、旧
`sr-lesson` 技能路径、数学旧生成指南、旧通用数学 prompt、旧流程样品及只访问
`/lesson/:id` iframe 产物的测试。

通过条件：活动说明、技能路由、模块事实和资源入口中不存在把这些对象当作当前数学生成路径的命中；已决定删除的旧文件和目录不存在。

# AC3：现行 S10Y 链路保持可用

检查：运行 `ld-s10y-lesson`、`ld-s10y-image`、`ld-s10y-answer` 的现有确定性测试；按 operations 的工作树命令启动应用，在本票端口用 Playwright 打开一个已发布的 `/card/:id` 页面并检查课文或练习可见且无错误态。

通过条件：目标测试通过；已有课程单元在浏览器中可访问，正文或练习至少一项非空，课程生成相关的现行技能和 `resources/s10y-lessons/` 产物仍存在。
