# Acceptance Checks

## AC1 第一章六课完整且边界正确

- 查询 edition 的 `book.json`、六个 lesson 目录和生产 `sr_lessons`，断言
  目标集合与权威 TOC 第一章展开结果完全一致；该集合是 `phy6-c1-s1` 至 `phy6-c1-s6`。
- 启动本地产品后从物理六年级目录逐课进入，断言六课可点击、标题和内容非空。
- 对发布前后数据做只读对账，断言本次发布集合不包含 `phy6-c2-*`，并对照 PDF 物理页 23
  确认第二章内容未进入第一章。不得把其他会话既有的课程误记为本工单发布。

通过条件：六课的页面、持久产物和数据库记录一致，第一章外没有新增发布。

## AC2 正文、物理关系和插图在产品与打印中可读

- 对照 PDF 物理页 12–22 检查每课正文、问题、单位、人物、图号和装置关系。
- 用 ticket-scoped Playwright 在 `1440x960` 和 `390x844` 检查六课正文及习题页：
  页面可达、无错误态、无横向溢出，全部图像有非空像素且可完整滚动查看。
- 图片覆盖集合从源清单及实际正文、习题对象推导，包含全部编号图和无编号肖像；分别报告
  正文图、习题图和打印图覆盖，拒绝漏图或以零张习题图代表整课检查通过。
- 生成并检查每课打印预览；核对图像无裁切、标签不小于 FigureSpec 声明的产品字号，
  deterministic/hybrid 图的断言、碰撞和边界报告均通过。
- 核对原书历史人物、图注与现代图身份对应，必要历史名词不被虚构人名、国名替代；
  生成肖像不声称是原始历史照片。

通过条件：所有课程在两种视口和打印预览中层次清楚，图文完整且不改变原书物理关系。

## AC3 题目、答案、交互和数学键盘完整

- 比较原始转写、edition exercises、answer keys、interactions 和数据库 JSONB，断言每课题数、
  题序和关联图一致，每题恰有一个答案键和一个交互规格。
- 运行 `check_product.mjs`，逐个答案输入检查数学键盘按钮可打开、按键只写入目标输入框；
  `free` 题同样必须覆盖，并验证中文解释可以输入和提交。
- 在 localhost 的全新匿名浏览器上下文提交样本，不能注入真实学习者 cookie：
  `auto` 存在时检查正确、错误样本；`ungraded` 检查返回不判正误且显示非空标准答案，
  不能显示错误判定。无 `auto` 题时明确记录不适用，不为验收篡改题型或新增题目。
- 第 1 节保持无题；第 6 节的无编号问题保留“原书无题号”的来源事实，交互序号只用于课内
  定位；跨课同号的问题和答案不得串联。无法定位 lesson 的书后答案不得自动套用。

通过条件：13 道原书问题均正确显示并可作答，没有缺答案、缺交互或缺数学键盘的输入框。

## AC4 共享流程缺陷已修复并留下可执行证据

- 运行针对 source manifest 映射、section 叶子卡片认领、每课题号作用域和历史人物保留规则的
  聚焦测试；同时覆盖无作用域书后答案的拒绝关联、无题/无编号题、正文图片覆盖与自由回答。
  原有数学全书题号的重号、缺号检查必须仍有效。
- 运行课程、图片和答案技能各自的 finalize/gate，以及
  `python3 ssot-resources/audit.py`、
  `python3 ssot-resources/soviet10year-textbooks/validate.py`、
  `cd app && npm run test && npm run build`。
- 检查统计记录包含开始/结束时间、分阶段耗时与等待/返工、课程与题数、源图和生成产物数、
  图片模型调用与重试数及可观测 token/费用。模型名称来自实际调用记录，不可观测的用量明确
  标注不可观测；估算费用与实测分开，不把估算称作实际账单。
- 持久资源只在 `ssot-resources`，工单过程记录在工单目录；`.tmp` 仅含可删除中间件。
  数据库发布、线上可读和代码部署分别报告，本工单不得把未部署描述成内容未上线。

通过条件：上述检查全部通过，工单分支已提交并推送，`52142` 预览保留运行。
