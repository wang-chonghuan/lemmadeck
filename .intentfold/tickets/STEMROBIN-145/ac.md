# Acceptance Checks

## 1. Legacy display numbers

- Run focused app and publisher tests proving a missing source-number field displays and
  republishes the existing stable number, while an explicit null stays blank.
- Start localhost on the ticket port and check representative 5m, 6a, and 6p cards at
  1440x960 and 390x844. Assert visible math numbers, a blank unnumbered physics item,
  exercise targeting, and an operable math keyboard.

## 2. Repeated physics numbering

- Run the assembly regression modeled on printed 6p page 20. Assert six exercises with
  faithful groups and display numbers, unique stable identities across repeated 1/2,
  deterministic repeated output, and unambiguous answer/interaction/figure joins.
- Run negative regressions proving a duplicate inside one group, a missing number inside one
  group, and an ambiguous figure owner are rejected.

## 3. Main lessons and supplemental reading

- Run catalog, assembly, and publication-order regressions. Assert all eight affected 6p main
  sections are cards before their supplemental readings, `phy6-c2-s4` is independently
  addressable, untranslated/unpublished entries remain disabled, and numbered math topic
  containers remain structural.
- In the localhost catalog, inspect the 6p hierarchy at both required viewports and confirm
  the main section and supplemental reading are separate rows under the same collapsible
  section.

## 4. Shared source and answer identity

- Run source-resolution checks proving both 6p and 7p resolve the manifest-declared
  `6-7p` PDF without `--pdf`.
- Finalize a page-faithful lesson/group sample containing repeated 1/2 and an unnumbered
  assignment, then prepare lesson answers and confirm each stable exercise receives only its
  matching captured answer.
- Re-finalize existing 5m and 6a captured answers and run the project mechanical defence from
  the Charter.
