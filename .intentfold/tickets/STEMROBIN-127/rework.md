# Rework

首次 handoff 后，根据产品中的实际阅读效果继续修正了首批两课：

- 将现代正文确定性拆成短段落，并用 `section_breaks` 表达少量真正的概念切换；
- 恢复数学交互规格对输入控件的优先级，确保需要公式输入的题显示数学键盘；
- 移除课文首行缩进，统一公式外数字/拉丁字母与 KaTeX 的字体，并将两者调整为中文正文的
  `1.10em`；
- 重新 render、按基础课程 → 答案 → 交互规格的顺序发布两课，并完成桌面、移动端检查；
- 将上述规则写回 `ld-s10y-lesson`，并增加正文结构、两条渲染路径的字体契约，以及基础
  课程重发保留 `answerKey`/`interaction` 的永久测试。

用户要求工单保持开放后，试产范围扩展到第三课 `alg6-c1-s2-n3`：

- 完成物理页 17–20 的忠实转写和装订，第三课共 5 个现代正文段落、练习 34–48；
- 生成两张确定性现代练习表，并按 `ld-s10y-image` 的规则保留英文可见标签；
- 正文使用两处概念分隔，现代例子改为密西西比河、孟菲斯和西雅图；
- 生成 15 份答案键和 15 份交互规格；第 47 题保持公式编辑器与数学键盘；
- 原书第 46 题书后答案有误：保留 `bookRaw`，按题面发布正确结果 `19.7` 并记录说明；
- 按基础课程 → 答案 → 交互规格的顺序写库，并通过桌面、移动端、答案保密和服务端判题验收。

第三课人工复核后继续修正练习区可读性：

- 为练习图增加稳定的 figure id 标记，仅将 `tbl-*` 表格放大到 640px；
- 窄屏使用表格自身横向滚动，不造成页面级横向溢出；
- 将 `a=-3` 一类分题作答标签从 12px 提升到 15px，并提高文字对比度。
